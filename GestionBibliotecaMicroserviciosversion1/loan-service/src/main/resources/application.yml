server:
  port: 8082

spring:
  application:
    name: loan-service
  datasource:
    url: jdbc:mysql://localhost:3306/loan_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
